int main()
{
    decl
        int temp, fileName, b;
        string input, output;
    enddecl
    begin
        input = "Pgm to run:";
        temp = exposcall("Write", -2, input);
        temp = exposcall("Read", -1, fileName);

        temp = exposcall("Exec", fileName);
        return 0;
    end
}